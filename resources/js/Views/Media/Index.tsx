import MediaIndex from './MediaIndex';

// Debug: log the imported component so we can see whether it's a function or an unexpected object
try { console.debug('MediaIndex resolved:', MediaIndex); } catch (e) { /* ignore in non-browser env */ }

export default MediaIndex;
